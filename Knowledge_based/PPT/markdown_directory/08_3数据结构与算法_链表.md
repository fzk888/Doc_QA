数据结构与算法
链表['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/0.jpg']***ppt换页识别符号***链表
链表判空 , 长度 , 遍历
链表增加结点
链表删除和查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/1.jpg']***ppt换页识别符号***1.了解顺序表存储方式的不足
2. 知道链表的存储方式
3. 理解链表结构并能够构建链表['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/2.jpg']***ppt换页识别符号***顺序表的不足
顺序表存储时需要连续的内存空间，当要扩充顺序表时会出现以下两种情况：
顺序表-内存充足时
内存充足时易找到连续的内存空间
顺序表
顺序表
占用
占用
顺序表-内存不足时
内存不充足时，不易找到连续的内存空间；完成不了扩展扩充
顺序表
顺序表
占用
占用
占用
占用['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/3.jpg']***ppt换页识别符号***链表
 不需要连续的存储空间
占用
0x11
0x14
0x12
10
占用
0x13
20
0x16
存储: [10 , 20 , 30 , 40]
0x14
占用
0x15
每个结点有2部分：元素域，下一个结点的内存地址（链接域）；最后一个结点链接域为None
30
0x18
0x16
占用
0x17
0x18
40['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/4.jpg']***ppt换页识别符号***链表结构
单链表(单向链表)是链表的一种形式，每个结点包含两个域：元素域和链接域 . 
这个链接指向链表中的下一个结点 , 而最后一个结点的链接域则指向一个空值None
下结点
链接域
表
元素域
head
单链表
item
next
None
单链表结点
①表元素域item用来存放具体的数据
②链接域next用来存放下一个结点的位置
③变量head指向链表的头结点（首结点）的位置，从head出发能找到表中的任意结点
['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/5.jpg']***ppt换页识别符号***结点代码实现
从面向对象的角度思考链表：应该有2个对象：结点对象SingleNode、链表对象SingLinkList
下一结点
链接域
表元素
域
class SingleNode(object):    """链表结点实现"""    def __init__(self, item):        # item: 存放元素        self.item = item        # next: 标识下一个结点        self.next = None
item
next
单链表结点
如果 node 是一个结点:
获取结点元素 : node.item
获取下一个结点 : node.next  ['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/6.jpg']***ppt换页识别符号***单链表代码的实现
head
单链表
None
is_empty(self) 链表是否为空
length(self) 链表长度
travel(self. ) 遍历整个链表
add(self, item) 链表头部添加元素
append(self, item) 链表尾部添加元素
insert(self, pos, item) 指定位置添加元素
remove(self, item) 删除节点
search(self, item) 查找节点是否存在

node2
node1
node3
class SingleLinkList(object):    """单链表的实现"""    def __init__(self, node=None):        # 首结点        self.head = node
通过append(node) 方法不断添加元素 , 最终实现单链表
['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/7.jpg']***ppt换页识别符号***1.顺序表存储方式的不足?
     顺序表需要连续的内存空间，当空间不足时，顺序表无法扩充
2.链表的存储方式是否需要连续的内存空间？
     不需要连续的内存空间
3.链表结构是什么样的？
    每个结点包含两个域：元素域和链接域 . 
        (1) 元素域存储当前结点的数据元素
        (2) 链接域存储链表中的下一个结点的位置['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/8.jpg']***ppt换页识别符号***链表
链表判空 , 长度 , 遍历
链表增加结点
链表删除和查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/9.jpg']***ppt换页识别符号***1、能够代码实现链表的判空 
2、能够代码实现链表的长度测定
3、能够代码实现链表的遍历['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/10.jpg']***ppt换页识别符号***链表的判空
class SingleLinkList(object):    """单链表的实现"""    def __init__(self, node=None):        # 首结点        self.head = node    def is_empty(self):        # 判断链表是否为空        if self.head == None:            return True        else:            return False
head
非空链表
None
head
空链表
None['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/11.jpg']***ppt换页识别符号***链表的长度测量
head
item
next
item
next
next
item
None
cur
count = 0
cur
count = 1

cur
count = 3

cur
count = 2

# 获取链表长度def length(self):    # 游标记录当前所在的位置    cur = self.head    # 记录链表的长度    count = 0    while cur is not None:        cur = cur.next        count += 1['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/12.jpg']***ppt换页识别符号***链表的遍历
head
item
next
item
next
next
item
None
cur
cur


cur

cur

# 遍历链表def travel(self):    cur = self.head    while cur is not None:        print(cur.item)        cur = cur.next['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/13.jpg']***ppt换页识别符号***1.编写代码实现：
链表的判空
链表的长度测定
链表的遍历['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/14.jpg']***ppt换页识别符号***链表
链表判空 , 长度 , 遍历
链表增加节点
链表删除和查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/15.jpg']***ppt换页识别符号***1、能够代码实现链表在头部增加结点
2、能够代码实现链表在尾部增加结点
3、能够代码实现链表在指定位置增加结点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/16.jpg']***ppt换页识别符号***add(item) 链表头部添加结点
head
# 头部增加结点def add(self, item):    # 新结点存储新数据    node = SingleNode(item)    node.next = self.head    self.head = node
next
item
next
item
next
item
None
new_node
item
next
None
注意: 一定按照顺序执行
1 new_node.next = head
2 head = new_node['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/17.jpg']***ppt换页识别符号***append(item) 链表尾部添加结点
# 尾部增加结点def append(self, item):    # 新结点存储新数据    node = SingleNode(item)    if self.is_empty():        self.head = node    else:        cur = self.head        while cur.next is not None:            cur = cur.next        # 当前结点后面连接新结点        cur.next = node
head
item
next
item
next
item
next
None
new_node
尾结点.next = 新节点
item
next
None['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/18.jpg']***ppt换页识别符号***append(item) 链表尾部添加结点
如何找到尾结点?
head
# 尾部增加结点def append(self, item):    # 新结点存储新数据    node = SingleNode(item)    if self.is_empty():        self.head = node    else:        cur = self.head        while cur.next is not None:            cur = cur.next        # 当前结点后面连接新结点        cur.next = node
item
next
item
next
item
next
None
cur
cur
cur
cur
方法?:
cur = head

while cur is not None:
       cur = cur.next
                  
最终cur指向的不是最后一个结点
而是None['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/19.jpg']***ppt换页识别符号***append(item) 链表尾部添加结点
如何找到尾结点?
head
# 尾部增加结点def append(self, item):    # 新结点存储新数据    node = SingleNode(item)    if self.is_empty():        self.head = node    else:        cur = self.head        while cur.next is not None:            cur = cur.next        # 当前结点后面连接新结点        cur.next = node
item
next
item
next
item
next
None
cur
cur
cur
方法:
cur = head

while cur.next is not None:
       cur = cur.next
                  ['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/20.jpg']***ppt换页识别符号***append(item) 链表尾部添加结点
head
# 尾部增加结点def append(self, item):    # 新结点存储新数据    node = SingleNode(item)    if self.is_empty():        self.head = node    else:        cur = self.head        while cur.next is not None:            cur = cur.next        # 当前结点后面连接新结点        cur.next = node
item
next
item
next
item
next
None
cur
方法:
cur = head

while cur.next is not None:
       cur = cur.next

cur.next = new_node
                  
item
next
None['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/21.jpg']***ppt换页识别符号***append(item) 链表尾部添加结点
如果链表为空链表？
head
# 尾部增加结点def append(self, item):    # 新结点存储新数据    node = SingleNode(item)    if self.is_empty():        self.head = node    else:        cur = self.head        while cur.next is not None:            cur = cur.next        # 当前结点后面连接新结点        cur.next = node
None
head= new_node
new_node
item
next
None['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/22.jpg']***ppt换页识别符号***# 指定位置增加结点def insert(self, pos, item):    # 头部添加新结点    if pos <= 0:        self.add(item)    # 尾部添加新结点    elif pos >= self.length():        self.append(item)    # 添加新结点    else:        # 游标        cur = self.head        # 计数        count = 0        # 新结点        node = SingleNode(item)        # 1 找到插入位置的前一个结点        while count < pos-1:            cur = cur.next            count += 1        # 2 完成插入新结点        node.next = cur.next        cur.next = node
Insert(pos,item)在指定位置添加结点
pos = 2 
head
item
next
item
next
item
next
None
插入结点指向下一个结点
cur
count = 2

cur
count = 0
cur
count = 1

找到插入之前的结点
new_node
item
next
None
cur
count = 2

思考：如果pos=0或pos大于链表长度怎么处理？['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/23.jpg']***ppt换页识别符号***1、代码实现
链表在头部增加结点
链表在尾部增加结点
链表在指定位置增加结点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/24.jpg']***ppt换页识别符号***链表
链表判空 , 长度 , 遍历
链表增加节点
链表删除和查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/25.jpg']***ppt换页识别符号***1、能够代码实现链表删除节点
2、能够代码实现链表查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/26.jpg']***ppt换页识别符号***remove(item) 删除节点
# 删除结点def remove(self, item):    # 游标    cur = self.head    # 辅助游标(指向前一个结点的游标)    pre = None    while cur is not None:        # 找到要删除的结点        if cur.item == item:            # 若删除是头结点            if cur == self.head:                self.head = cur.next            else:                pre.next = cur.next            return        # 没有找到要删除的元素        else:            pre = cur            cur = cur.next
head
next
next
item
item
item
next
None
cur['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/27.jpg']***ppt换页识别符号***remove(item) 删除节点
# 删除结点def remove(self, item):    # 游标    cur = self.head    # 辅助游标(指向前一个结点的游标)    pre = None    while cur is not None:        # 找到要删除的结点        if cur.item == item:            # 若删除是头结点            if cur == self.head:                self.head = cur.next            else:                pre.next = cur.next            return        # 没有找到要删除的元素        else:            pre = cur            cur = cur.next
head
item
next
item
next
item
next
None
cur
pre
cur
pre['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/28.jpg']***ppt换页识别符号***search(item) 查找节点是否存在
# 查找结点是否存在def search(self, item):    # 游标    cur = self.head    while cur is not None:        if cur.item == item:            return True        cur = cur.next    return False
head
item
next
item
next
item
next
None
cur['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/29.jpg']***ppt换页识别符号***1、能够代码实现
链表删除节点
链表查找节点['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/30.jpg']***ppt换页识别符号***顺序表和链表的比较
顺序表
链表
占用
0x11
0x11
0
10
0
0x14
0x12
10
20
0x12
1
占用
0x13
0x13
30
2
1
20
0x16
0x14
40
3
0x14
占用
0x15
0x15
50
2
30
0x18
0x16
60
0x16
占用
0x17
0x17
70
3
0x18
40
80
0x18
链表失去了顺序表随机读取的优点，链表由于增加了结点的连接域，空间开销比较大，但对存储空间的使用要相对灵活。['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/31.jpg']***ppt换页识别符号***顺序表和链表的比较['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/32.jpg']***ppt换页识别符号***['./Knowledge_based\\PPT\\images\\08_3数据结构与算法_链表/33.jpg']***ppt换页识别符号***